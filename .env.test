NODE_ENV=production
# Whether to open mock
VITE_USE_MOCK = true

# public path
VITE_PUBLIC_PATH = /tms/

# Delete console
VITE_DROP_CONSOLE = true

# Whether to enable gzip or brotli compression
# Optional: gzip | brotli | none
# If you need multiple forms, you can use `,` to separate
VITE_BUILD_COMPRESS = 'none'

# Whether to delete origin files when using compress, default false
VITE_BUILD_COMPRESS_DELETE_ORIGIN_FILE = false

# Basic interface address SPA / spa 基础接口地址
VITE_GLOB_API_URL=/tms_api

# File upload address， optional
# It can be forwarded by nginx or write the actual address directly
VITE_GLOB_UPLOAD_URL=/tms_api/sys/remittance/file/uploading

# Interface prefix / 接口前缀
VITE_GLOB_API_URL_PREFIX=

# Whether to enable image compression / 是否启用图片压缩
VITE_USE_IMAGEMIN= true

# use pwa / 使用pwd
VITE_USE_PWA = false

# Is it compatible with older browsers / 是否兼容旧版本浏览器
VITE_LEGACY = false

